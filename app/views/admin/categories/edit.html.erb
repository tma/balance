<% content_for :title, "Edit Category - Admin" %>

<% content_for :breadcrumbs do %>
  <%= ui_breadcrumbs do %>
    <%= ui_breadcrumb_first "Settings" %>
    <%= ui_breadcrumb_item "Categories", admin_categories_path %>
    <%= ui_breadcrumb_item @category.name, admin_category_path(@category) %>
  <% end %>
<% end %>

<div>
  <div class="flex items-center h-10 mb-6">
    <h1 class="<%= ui_title_class %>">Edit Category</h1>
  </div>
  <%= render "form", category: @category %>
</div>
